{
  "name": "sim-idx",
  "version": "0.0.1",
  "private": false,
  "dependencies": {
    "drizzle-orm": "^0.44.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  },
  "scripts": {
    "build": "rm -rf dist && esbuild --bundle src/index.ts --platform=node --outdir=dist --sourcemap"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ]
}
